apiVersion: batch/v1
kind: Job
metadata:
  name: dna-short-every2
spec:
  template:
    spec:
      containers:
      - name: gpu-container
        image: gitlab-registry.nrp-nautilus.io/prp/jupyter-stack/prp:latest
        command: ["/bin/bash", "-c"]
        args:
          -
            cd /dna-volume/bonito;
            git pull;
            source venv3/bin/activate;
            ./scripts/dyn-skip-shorten-every2.sh
            |& tee /dna-volume/training-logs/dyn-skip-shorten-pretrained-student-every2epochs.log
        volumeMounts:
        - mountPath: /dna-volume
          name: big-dna-volume
        resources:
          limits:
            memory: 56Gi
            cpu: "6"
            nvidia.com/gpu: "1"
          requests:
            memory: 50Gi
            cpu: "1"
            nvidia.com/gpu: "1"    
      volumes:
        - name: big-dna-volume
          persistentVolumeClaim:
            claimName: big-dna-volume
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-GeForce-RTX-2080-Ti

  backoffLimit: 2
