apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: big-dna-volume
  namespace: brevitas
spec:
  storageClassName: rook-cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
